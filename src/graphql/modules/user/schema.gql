type User {
  _id: ID!
  name: String!
  email: String
  password: String
  recharging: Boolean
}

input UserInput {
  name: String!
  email: String!
  password: String!
}

type Query {
  user: User!
}

type Mutation {
  createUser(data: UserInput!): User!
}